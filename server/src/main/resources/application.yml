server:
  port: 10002
  servlet:
    context-path: /lingdong-lw-server

spring:
  application:
    name: lingdong-lw-server
  datasource:
    url: ${PAYROLL_DB_URL:jdbc:mysql://8.156.80.3:3306/lingdong_payroll?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai}
    username: ${PAYROLL_DB_USERNAME:root}
    password: ${PAYROLL_DB_PASSWORD:Colinzs@aly520}
  flyway:
    enabled: true
    locations: classpath:db/migration

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true

payroll:
  jwt-secret: ${PAYROLL_JWT_SECRET:change-this-in-production-change-this-in-production}
  export-dir: ${PAYROLL_EXPORT_DIR:/home/apps/lingdong-lw/exports}
